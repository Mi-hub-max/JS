const ROOT_PRODUCTS = document.getElementById("products");
const ROOT_HEADER = document.getElementById("header");
const ROOT_PRODUCTS_BASKET = document.getElementById("productsBasket");
const ROOT_FILTER_PRODUCTS = document.getElementById("filterProducts");
